<!-- Per-repo agent-config retired → global -->

# Agent Configuration (redirect)

> Per-repo agent config retired 2026-07-08. The authoritative agent configuration is the user-global master at `~/.codex/AGENTS.md` (Windows: `C:\Users\RyanClanton\.codex\AGENTS.md`).
> Recovery: the prior per-sub AGENTS.md (now removed) is fully recoverable from this sub-repo's git history at `git log --all --diff-filter=D -- AGENTS.md`.
