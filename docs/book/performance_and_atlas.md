# SIMD, GPU, and Backend Migration

Performance-oriented examples are grouped with Atlas migration references so
runtime behavior and architecture transitions can be validated together:

- SIMD acceleration paths (`hermes`)
- GPU availability and execution (`hephaestus`)
- spectral and backend-level throughput checks

